import type { Metadata } from "next";
import HoraPage from "@/features/hora/HoraPage";
import { horaMetadata } from "@/features/hora/metadata";

export const metadata: Metadata = horaMetadata;
export default HoraPage;
