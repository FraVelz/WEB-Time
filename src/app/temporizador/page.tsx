import type { Metadata } from "next";
import TemporizadorPage from "@/features/temporizador/TemporizadorPage";
import { temporizadorMetadata } from "@/features/temporizador/metadata";

export const metadata: Metadata = temporizadorMetadata;
export default TemporizadorPage;
