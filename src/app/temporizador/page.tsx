import type { Metadata } from "next";
import TemporizadorPage from "@/features/timer/pages/TemporizadorPage";
import { temporizadorMetadata } from "@/features/timer/metadata";

export const metadata: Metadata = temporizadorMetadata;
export default TemporizadorPage;
